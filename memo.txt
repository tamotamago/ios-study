
Master-Detail Application を選択
StroryBoardとARCをオンにする

ある程度、最初からひな形はできているみたい

JSON Parser or XML Parser
を入れる。使い方はあまり変わらないだろうから、JSON Parserを使う？
Frameworkに入っている雰囲気

まずは、データをフェッチしてくる
iTunes Store Rss
でいい感じのRSsフィードジェネレータがあったりするから、これを使って
URLを作る。最後のデータのみをxml -> json に帰る

ex ) 
https://itunes.apple.com/jp/rss/topfreeapplications/limit=25/json

一度 wget なり curl なりしてきちんと取れるか確認する

データを取ってくるコードを書く.
エントリポイントは本当はAppDelegateだけど、実質的にはviewDidloadの方が分かりやすい
[NSURLConnection sendAsynchronousRequest]してもいいs
delegateメソッド書いてもいいし、
メソッドが増えると面倒だから、sendAsynchが便利かもしれないし、
GCD使って別スレッドの方が分かりよいかもしれない

一度NSLogして出力されることを確認

基本的なデータ型
文字列型 : NSString
データ型 : NSData
配列    :  NSArray
ハッシュ : NSDictioanry


itunesのだと、データはとってこれるけどテーブルビューとして楽しくない

FlickrのAPIを使うサンプルだと
http://www.flickr.com/services/feeds/photos_public.gne?format=json&nojsoncallback=1
みたいなURLで取って来れる

------------------要らない部分の削除------------------
